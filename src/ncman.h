#ifndef NCMAN_H
#define NCMAN_H

#include <ncurses.h>

typedef struct Menu Menu;               // holds windows (buttons and previews) and pointers to other Menus/CommandMenus
                                        // is declared on the heap, so its programmers responsibility to delete

typedef struct CommandMenu CommandMenu; // 



#endif // NCMAN_H
